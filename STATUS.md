# Release status

Current status: **Release-grade** — the pipeline package, 44 offline unit tests, `MODEL_CARD.md`, and the standalone `E2E` tutorial `tutorials/molformer_chemistry_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks and the remote-code boundary rule). The exact committed notebook blob completed its full default path in a clean Kaggle Tesla T4 runtime, including digest verification before importing the pinned remote-code files; `docs/release-verification.md` records the evidence. Unit tests and static conformance checks run in GitHub Actions. Release-grade describes the verified tutorial carrier and does not imply benchmark or production-readiness evidence.

DIMER hosting carries a separate open gate: this checkpoint cannot be loaded without `trust_remote_code=True`, which MODEL_ASSET_SPEC §12 (RC6) treats as not ordinarily qualified until ruled on. See the *DIMER deployment notes* section of `MODEL_CARD.md`.
